results_qkmeans_ibm = run_qkmeans_5point_ibm_notebook(
    sizes=(2, 3, 4),
    steps=50,                                   # max_iter for kernel K-means
    trials=10,
    acc_tol=0.5,
    excel_filename="Filtered CO2 intensity 236 Countries.xlsx",
    device_power_watts=65.0,                   
    pue=1.2,                                   
    year_select="latest",
    outdir="carbon_by_country",
    backend_name="ibm_torino",                 # we can change to any actual backend
    shots=1024,                                # light but reasonable shots
)

results_qkmeans_ibm["perf_df"].head()
